#
# Copyright contributors to the Galasa project 
#
apiVersion: v1
kind: Service
metadata:
  name: couchdb-external
  labels:
    app: couchdb-external
spec:
  type: NodePort
  selector:
    app: ras
  ports:
  - port: 5984
    targetPort: 5984
    protocol: TCP
    name: http
