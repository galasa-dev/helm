#
# Copyright contributors to the Galasa project 
#
apiVersion: v1
kind: Service
metadata:
  name: etcd-external
  labels:
    app: etcd-external
spec:
  type: NodePort
  selector:
    app: etcd
  ports:
  - port: 2379
    targetPort: 2379
    protocol: TCP
    name: client
